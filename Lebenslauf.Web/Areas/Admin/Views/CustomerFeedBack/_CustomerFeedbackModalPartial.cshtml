@model Lebenslauf.Domain.ViewModels.CustomerFeedBack.CreateOrEditCustomerFeedbackViewModel

@{
    bool isCreate = Model.Id == 0 ? true : false;
}


<form id="CustomerFeedbackForm" asp-area="Admin" asp-controller="CustomerFeedback" asp-action="SubmitCustomerFeedbackFormModal"
      data-ajax="true" data-ajax-begin="StartLoading()" data-ajax-success="CustomerFeedbackFormSubmited">

    <div class="modal-header">
        <h5 class="modal-title">@(isCreate ? "Erstellen" : "Bearbeiten")</h5>
        <button type="button" class="close" data-dismiss="modal">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>


    <div class="modal-body">

        <div class="row">
            <div class="col-12">

                <input type="hidden" asp-for="Avatar">

                @if (!isCreate)
                {
                    <input type="hidden" asp-for="Id" />
                }

                <div class="form-group">
                    <label asp-for="Name"></label>
                    <div class="controls">
                        <input type="text" asp-for="Name" class="form-control">
                    </div>
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Order"></label>
                    <div class="controls">
                        <input type="number" asp-for="Order" class="form-control">
                    </div>
                    <span asp-validation-for="Order" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="Description"></label>
                    <div class="controls">
                        <textarea asp-for="Description" class="form-control"></textarea>
                    </div>
                    <span asp-validation-for="Description" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label for="image">Avatar</label>
                    <div class="controls">
                        <input type="file" name="image" id="image" class="form-control" />
                    </div>
                    <span asp-validation-for="Avatar" class="text-danger"></span>
                </div>

                <div>
                    <button type="submit" class="btn btn-bold btn-pure float-right @(isCreate ? "btn-success" : "btn-warning")">@(isCreate ? "Erstellen" : "Bearbeitung")</button>
                </div>

            </div>
        </div>

    </div>

</form>


<script>
    $("#image").change(function () {

        const imageFile = $('input#image')[0].files[0]

        if (imageFile.name.split('.').pop() === "png" || imageFile.name.split('.').pop() === "jpeg" || imageFile.name.split('.').pop() === "jpg") {

            const fd = new FormData();
            fd.append("file", imageFile);

            $.ajax({
                type: "Post",
                url: "/Admin/CustomerFeedback/UploadCustomerFeedbackImageAjax",
                enctype: "multipart/form-data",
                data: fd,
                beforeSend: function () {
                    ShowMessage("NOtfication", "Bitte warten Sie, bis der Upload abgeschlossen ist. ...", "info")
                },
                success: function (response) {
                    if (response.status === "Success") {
                        ShowMessage("Notfication", " Die gewünschte Datei wurde erfolgreich hochgeladen..", "success");
                        $("#Avatar").val(response.imageName);
                    }
                    else if (response.status === "Error") {
                        ShowMessage("Error", "Das Dateiformat muss jpeg ، jpeg Oder png Sein", "error")
                    }
                },
                error: function () {
                    ShowMessage("Error", " Beim Hochladen der Datei ist ein Fehler aufgetreten..", "error")
                },
                cache: false,
                contentType: false,
                processData: false
            })
        }
        else {
            ShowMessage("Error", "Das Dateiformat mussد jpeg ، jpeg Oder png Sein", "error")
        }


    });
</script>